# Bazel settings for the standalone examples workspace.

# Keep Java toolchain hermetic so examples don't require a host JDK.
build --java_runtime_version=remotejdk_11

# Debian snapshot can be flaky; retries make tests more robust.
common --experimental_repository_downloader_retries=10
